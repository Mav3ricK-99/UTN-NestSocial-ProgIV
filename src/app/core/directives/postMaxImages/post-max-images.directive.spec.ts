import { PostMaxImagesDirective } from './post-max-images.directive';

describe('PostMaxImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new PostMaxImagesDirective();
    expect(directive).toBeTruthy();
  });
});
