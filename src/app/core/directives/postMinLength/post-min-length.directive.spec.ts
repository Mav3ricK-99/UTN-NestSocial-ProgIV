import { PostMinLengthDirective } from './post-min-length.directive';

describe('PostMinLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new PostMinLengthDirective();
    expect(directive).toBeTruthy();
  });
});
