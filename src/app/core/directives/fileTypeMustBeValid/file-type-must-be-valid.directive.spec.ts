import { FileTypeMustBeValidDirective } from './file-type-must-be-valid.directive';

describe('FileTypeMustBeValidDirective', () => {
  it('should create an instance', () => {
    const directive = new FileTypeMustBeValidDirective();
    expect(directive).toBeTruthy();
  });
});
