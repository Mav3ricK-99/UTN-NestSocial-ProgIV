import { PasswordsMustBeSecureDirective } from './passwords-must-be-secure.directive';

describe('PasswordsMustBeSecureDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordsMustBeSecureDirective();
    expect(directive).toBeTruthy();
  });
});
