import { PostMaxLengthDirective } from './post-max-length.directive';

describe('PostMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new PostMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
